import {Transform} from './transform.js';

export class Mesh extends Transform {
    constructor(gl, {

    } = {}) {
        super(gl);
        this.gl = gl;
    }

    draw({
        camera,
         } = {}) {

    }

}